<!-- INSTAPIC EMERGENCY MOBILE/NAV/VENUE FIX -->
<style>
/* stop sideways scroll site-wide */
html, body {
  width: 100%;
  max-width: 100%;
  overflow-x: hidden !important;
}

/* bring menu/header back */
header,
.site-header,
.nav,
.navbar,
.mobile-nav,
.mobile-menu,
.menu,
.menu-toggle,
button.menu-toggle,
.menu-button,
.hamburger {
  display: flex !important;
  visibility: visible !important;
  opacity: 1 !important;
  pointer-events: auto !important;
}

header,
.site-header,
.nav,
.navbar {
  width: 100% !important;
  max-width: 100vw !important;
  overflow: hidden !important;
}

/* mobile hero fix */
@media (max-width: 760px) {
  .hero {
    width: 100vw !important;
    max-width: 100vw !important;
    overflow: hidden !important;
    padding-left: 22px !important;
    padding-right: 22px !important;
  }

  .hero-content,
  .hero-inner,
  .hero-text {
    width: calc(100vw - 44px) !important;
    max-width: calc(100vw - 44px) !important;
    overflow: hidden !important;
  }

  .hero h1 {
    font-size: clamp(36px, 11.5vw, 50px) !important;
    line-height: 0.94 !important;
    letter-spacing: -0.035em !important;
    width: 100% !important;
    max-width: 100% !important;
    white-space: normal !important;
    overflow-wrap: normal !important;
  }

  .hero p,
  .hero .lead {
    font-size: 16.5px !important;
    line-height: 1.5 !important;
    width: 100% !important;
    max-width: 100% !important;
  }

  .hero .kicker,
  .kicker {
    font-size: 11px !important;
    line-height: 1.55 !important;
    letter-spacing: .18em !important;
    white-space: normal !important;
  }

  .hero .btn,
  .hero a.btn {
    width: 100% !important;
    max-width: 100% !important;
  }
}

/* restore venue names on desktop and mobile */
.venue-list,
.area-list,
.venue-pills,
.trusted-venues,
.venues-grid,
.venue-grid {
  display: flex !important;
  flex-wrap: wrap !important;
  gap: 12px !important;
  justify-content: center !important;
  visibility: visible !important;
  opacity: 1 !important;
  max-height: none !important;
  overflow: visible !important;
}

.venue-list span,
.area-list span,
.venue-pills span,
.trusted-venues span,
.venues-grid span,
.venue-grid span,
.venue-list a,
.area-list a,
.venue-pills a,
.trusted-venues a,
.venues-grid a,
.venue-grid a {
  display: inline-flex !important;
  visibility: visible !important;
  opacity: 1 !important;
  width: auto !important;
  max-width: 100% !important;
  white-space: normal !important;
}

/* compact venue names on phone */
@media (max-width: 760px) {
  .venue-list,
  .area-list,
  .venue-pills,
  .trusted-venues,
  .venues-grid,
  .venue-grid {
    display: grid !important;
    grid-template-columns: 1fr 1fr !important;
    gap: 8px !important;
    justify-content: stretch !important;
    max-height: none !important;
  }

  .venue-list span,
  .area-list span,
  .venue-pills span,
  .trusted-venues span,
  .venues-grid span,
  .venue-grid span,
  .venue-list a,
  .area-list a,
  .venue-pills a,
  .trusted-venues a,
  .venues-grid a,
  .venue-grid a {
    width: 100% !important;
    padding: 9px 7px !important;
    font-size: 9.5px !important;
    line-height: 1.25 !important;
    letter-spacing: .08em !important;
    text-align: center !important;
    justify-content: center !important;
  }
}
</style>
